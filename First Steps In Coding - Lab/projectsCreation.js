function projectsCreation(architectName, numOfProject) {

    let oneProject = 3;
    let totatTimeOfProject = numOfProject * oneProject;

    console.log(`The architect ${architectName} will need ${totatTimeOfProject} hours to complete ${numOfProject} project/s.`);
    
}

projectsCreation("Sanya", 9);